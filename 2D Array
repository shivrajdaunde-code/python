matrix = [
    [1, 2, 3],
    [4, 5, 6],
    [7, 8, 9]
]

print(matrix[0][0])     
print(matrix[1][2])     
print(matrix[2][1])     

rows = len(matrix)
cols = len(matrix[0])

for i in range(rows):
    for j in range(cols):
        print(matrix[i][j], end=" ")
    print()

# Updating
matrix[1][1] = 50

print(matrix)
